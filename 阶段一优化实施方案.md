# 阶段一优化实施方案

## 文档信息
- **创建时间**: 2026-04-30
- **目的**: 实施简单检索层，优化LLM调用效率和准确性
- **预计工期**: 1-2周
- **状态**: 待实施

---

## 优化目标

### 核心改进
1. ✅ 添加BM25检索层，减少LLM输入量
2. ✅ 限制输入chunk数量不超过30个
3. ✅ 优化LLM prompt，强制引用原文
4. ✅ 添加debug工具，提升可调试性

### 预期效果
- **成本降低**: 50-60%（Token从15000降至6000）
- **准确率提升**: 10-15%（强制引用原文）
- **响应速度**: 提升20-30%（减少LLM处理量）
- **可调试性**: 大幅提升（完整的debug信息）

---

## 技术方案

### 1. 简单检索层架构

```
ContentRetriever
    ↓
[新增] SimpleRetriever
    ↓ 1. Chunk拆分
    ↓ 2. BM25索引
    ↓ 3. 检索Top 30
    ↓
LLM (优化后的prompt)
    ↓
[新增] DebugLogger
```

### 2. Chunk拆分策略

#### 拆分规则
- **段落边界**: 优先按段落（\n\n）拆分
- **句子边界**: 段落过长时按句子（。！？）拆分
- **最大长度**: 每个chunk不超过500字符
- **最小长度**: 每个chunk至少100字符（避免碎片）
- **重叠窗口**: 相邻chunk重叠50字符（保持上下文）

#### Chunk元数据
```typescript
interface Chunk {
  id: string;              // 唯一标识
  content: string;         // 文本内容
  sourceUrl: string;       // 来源URL
  chunkIndex: number;      // 在文档中的位置
  pageType: string;        // 'main' | 'subpage' | 'document'
  score?: number;          // BM25评分
}
```


---

## 详细实现

### 1. SimpleRetriever类实现

```typescript
import natural from 'natural';

/**
 * 简单检索器 - 使用BM25算法
 */
export class SimpleRetriever {
  private tfidf: natural.TfIdf;
  private chunks: Chunk[] = [];
  
  constructor() {
    this.tfidf = new natural.TfIdf();
  }

  /**
   * 将内容拆分为chunks
   */
  chunkContent(
    content: string,
    sourceUrl: string,
    pageType: 'main' | 'subpage' | 'document'
  ): Chunk[] {
    const chunks: Chunk[] = [];
    
    // 1. 按段落拆分
    const paragraphs = content.split(/\n\n+/).filter(p => p.trim().length > 0);
    
    let chunkIndex = 0;
    for (const paragraph of paragraphs) {
      // 2. 如果段落过长，按句子拆分
      if (paragraph.length > 500) {
        const sentences = paragraph.split(/([。！？])/);
        let currentChunk = '';
        
        for (let i = 0; i < sentences.length; i += 2) {
          const sentence = sentences[i] + (sentences[i + 1] || '');
          
          if (currentChunk.length + sentence.length > 500) {
            if (currentChunk.length >= 100) {
              chunks.push(this.createChunk(
                currentChunk,
                sourceUrl,
                pageType,
                chunkIndex++
              ));
            }
            currentChunk = sentence;
          } else {
            currentChunk += sentence;
          }
        }
        
        if (currentChunk.length >= 100) {
          chunks.push(this.createChunk(
            currentChunk,
            sourceUrl,
            pageType,
            chunkIndex++
          ));
        }
      } else if (paragraph.length >= 100) {
        // 3. 段落长度适中，直接作为chunk
        chunks.push(this.createChunk(
          paragraph,
          sourceUrl,
          pageType,
          chunkIndex++
        ));
      }
    }
    
    return chunks;
  }

  /**
   * 创建chunk对象
   */
  private createChunk(
    content: string,
    sourceUrl: string,
    pageType: string,
    chunkIndex: number
  ): Chunk {
    return {
      id: `${sourceUrl}_${chunkIndex}`,
      content: content.trim(),
      sourceUrl,
      chunkIndex,
      pageType
    };
  }

  /**
   * 建立BM25索引
   */
  buildIndex(chunks: Chunk[]): void {
    this.chunks = chunks;
    this.tfidf = new natural.TfIdf();
    
    // 为每个chunk添加到TF-IDF索引
    chunks.forEach(chunk => {
      this.tfidf.addDocument(chunk.content);
    });
    
    console.log(`[SimpleRetriever] Built index with ${chunks.length} chunks`);
  }

  /**
   * 检索相关chunks
   */
  retrieve(query: string, topK: number = 30): Chunk[] {
    if (this.chunks.length === 0) {
      return [];
    }
    
    // 使用TF-IDF计算相关性
    const scores: Array<{ chunk: Chunk; score: number }> = [];
    
    this.tfidf.tfidfs(query, (i, score) => {
      if (i < this.chunks.length) {
        scores.push({
          chunk: { ...this.chunks[i], score },
          score
        });
      }
    });
    
    // 按分数排序并返回Top K
    const topChunks = scores
      .sort((a, b) => b.score - a.score)
      .slice(0, topK)
      .map(item => item.chunk);
    
    console.log(
      `[SimpleRetriever] Retrieved ${topChunks.length} chunks for query: "${query}"`
    );
    
    return topChunks;
  }

  /**
   * 批量检索多个关键词
   */
  retrieveMultiple(
    keywords: string[],
    topKPerKeyword: number = 10
  ): Map<string, Chunk[]> {
    const results = new Map<string, Chunk[]>();
    
    keywords.forEach(keyword => {
      const chunks = this.retrieve(keyword, topKPerKeyword);
      results.set(keyword, chunks);
    });
    
    return results;
  }
}
```


---

### 2. 优化后的LLM Prompt

#### 新Prompt模板

```typescript
/**
 * 构建优化后的LLM prompt
 */
private buildOptimizedPrompt(
  keyword: string,
  chunks: Chunk[]
): string {
  // 格式化chunks
  const formattedChunks = chunks
    .map((chunk, index) => {
      return `[Chunk ${index + 1} | ${chunk.sourceUrl}]\n${chunk.content}`;
    })
    .join('\n\n');

  return `关键词: ${keyword}

以下是可能相关的内容片段：

${formattedChunks}

任务：
1. 判断关键词"${keyword}"是否在上述内容中存在
2. 如果存在，给出该关键词的定义/解释/描述
3. 必须引用原文句子（完整的句子，不要截断）
4. 必须提供来源URL
5. 如果不存在，明确说"该关键词在提供的内容中不存在"

输出格式（严格JSON）：
{
  "keyword": "${keyword}",
  "found": true/false,
  "definition": "关键词的定义/解释（如果found为false，此字段为空字符串）",
  "quotedSentence": "引用的原文句子（完整句子）",
  "sourceUrl": "来源URL",
  "confidence": 0.0-1.0
}

注意：
- quotedSentence必须是原文中的完整句子，不能修改或截断
- sourceUrl必须是实际的来源URL，格式为 [Chunk X | URL] 中的URL
- 如果found为false，quotedSentence和sourceUrl都应为空字符串
- confidence表示答案的可信度（0.0-1.0）`;
}
```

#### System Prompt

```typescript
const SYSTEM_PROMPT = `你是一个金融合规政策分析专家。

你的任务是：
1. 在提供的内容片段中查找指定关键词
2. 提取关键词的定义、解释或描述
3. 必须引用原文句子作为证据
4. 必须提供准确的来源URL

重要规则：
- 只能基于提供的内容片段回答，不能使用外部知识
- 引用的句子必须是原文中的完整句子
- 如果内容中没有该关键词，必须明确说明
- 返回严格的JSON格式，不要包含任何其他文本`;
```


---

### 3. Debug工具实现

```typescript
/**
 * Debug信息接口
 */
interface DebugInfo {
  keyword: string;
  timestamp: string;
  websiteUrl: string;
  
  // 内容获取阶段
  contentFetch: {
    mainPageSize: number;
    subPagesCount: number;
    documentsCount: number;
    totalContentSize: number;
  };
  
  // Chunk拆分阶段
  chunking: {
    totalChunks: number;
    chunkSizes: number[];
    avgChunkSize: number;
  };
  
  // 检索阶段
  retrieval: {
    retrievedChunks: Array<{
      id: string;
      content: string;
      sourceUrl: string;
      score: number;
    }>;
    topScore: number;
    avgScore: number;
  };
  
  // LLM调用阶段
  llmCall: {
    promptTokens: number;
    completionTokens: number;
    totalTokens: number;
    duration: number;
    cost: number;
  };
  
  // LLM响应
  llmAnswer: {
    keyword: string;
    found: boolean;
    definition: string;
    quotedSentence: string;
    sourceUrl: string;
    confidence: number;
  };
  
  // 验证结果
  validation: {
    quotedSentenceValid: boolean;
    sourceUrlValid: boolean;
    warnings: string[];
  };
}

/**
 * Debug日志记录器
 */
export class DebugLogger {
  private debugInfo: Partial<DebugInfo> = {};
  
  constructor(keyword: string, websiteUrl: string) {
    this.debugInfo = {
      keyword,
      timestamp: new Date().toISOString(),
      websiteUrl
    };
  }
  
  /**
   * 记录内容获取信息
   */
  logContentFetch(
    mainPageSize: number,
    subPagesCount: number,
    documentsCount: number,
    totalContentSize: number
  ): void {
    this.debugInfo.contentFetch = {
      mainPageSize,
      subPagesCount,
      documentsCount,
      totalContentSize
    };
  }
  
  /**
   * 记录chunk拆分信息
   */
  logChunking(chunks: Chunk[]): void {
    const chunkSizes = chunks.map(c => c.content.length);
    const avgChunkSize = chunkSizes.reduce((a, b) => a + b, 0) / chunkSizes.length;
    
    this.debugInfo.chunking = {
      totalChunks: chunks.length,
      chunkSizes,
      avgChunkSize: Math.round(avgChunkSize)
    };
  }
  
  /**
   * 记录检索信息
   */
  logRetrieval(retrievedChunks: Chunk[]): void {
    const scores = retrievedChunks.map(c => c.score || 0);
    const topScore = Math.max(...scores);
    const avgScore = scores.reduce((a, b) => a + b, 0) / scores.length;
    
    this.debugInfo.retrieval = {
      retrievedChunks: retrievedChunks.map(c => ({
        id: c.id,
        content: c.content.substring(0, 200) + '...', // 只保留前200字符
        sourceUrl: c.sourceUrl,
        score: c.score || 0
      })),
      topScore,
      avgScore
    };
  }
  
  /**
   * 记录LLM调用信息
   */
  logLLMCall(
    promptTokens: number,
    completionTokens: number,
    duration: number
  ): void {
    const totalTokens = promptTokens + completionTokens;
    const cost = this.calculateCost(promptTokens, completionTokens);
    
    this.debugInfo.llmCall = {
      promptTokens,
      completionTokens,
      totalTokens,
      duration,
      cost
    };
  }
  
  /**
   * 记录LLM响应
   */
  logLLMAnswer(answer: any): void {
    this.debugInfo.llmAnswer = answer;
  }
  
  /**
   * 验证LLM响应
   */
  validate(chunks: Chunk[]): void {
    const warnings: string[] = [];
    let quotedSentenceValid = false;
    let sourceUrlValid = false;
    
    if (this.debugInfo.llmAnswer) {
      const { quotedSentence, sourceUrl } = this.debugInfo.llmAnswer;
      
      // 验证引用句子是否存在于chunks中
      quotedSentenceValid = chunks.some(chunk => 
        chunk.content.includes(quotedSentence)
      );
      
      if (!quotedSentenceValid && quotedSentence) {
        warnings.push('引用的句子在原文中不存在');
      }
      
      // 验证sourceUrl是否有效
      sourceUrlValid = chunks.some(chunk => 
        chunk.sourceUrl === sourceUrl
      );
      
      if (!sourceUrlValid && sourceUrl) {
        warnings.push('来源URL无效');
      }
      
      // 检查confidence
      if (this.debugInfo.llmAnswer.confidence < 0.5) {
        warnings.push('LLM置信度较低（< 0.5）');
      }
    }
    
    this.debugInfo.validation = {
      quotedSentenceValid,
      sourceUrlValid,
      warnings
    };
  }
  
  /**
   * 获取完整的debug信息
   */
  getDebugInfo(): DebugInfo {
    return this.debugInfo as DebugInfo;
  }
  
  /**
   * 输出debug信息到日志
   */
  print(): void {
    console.log('\n========== DEBUG INFO ==========');
    console.log(JSON.stringify(this.debugInfo, null, 2));
    console.log('================================\n');
  }
  
  /**
   * 保存debug信息到文件（可选）
   */
  async saveToFile(filepath: string): Promise<void> {
    const fs = require('fs').promises;
    await fs.writeFile(
      filepath,
      JSON.stringify(this.debugInfo, null, 2),
      'utf-8'
    );
  }
  
  /**
   * 计算LLM调用成本
   */
  private calculateCost(promptTokens: number, completionTokens: number): number {
    // Claude Sonnet 4 定价（示例）
    const promptCost = (promptTokens / 1000) * 0.003;  // $0.003 per 1K tokens
    const completionCost = (completionTokens / 1000) * 0.015;  // $0.015 per 1K tokens
    return promptCost + completionCost;
  }
}
```


---

### 4. 集成到ContentRetriever

```typescript
/**
 * 优化后的llmWebSearch方法
 */
private async llmWebSearch(
  websiteUrl: string,
  keywords: string[],
  mainPageContent: string,
  subPageContents: Array<{ url: string; content: string }>,
  documentContents: Array<{ url: string; content: string }>,
  pageLinks: string[],
  documentLinks: string[]
): Promise<{
  keywordMatches: KeywordMatch[];
  documentResults: DocumentRetrievalResult[];
  debugInfo?: DebugInfo[];
}> {
  const startTime = Date.now();
  const debugInfoList: DebugInfo[] = [];
  
  // 1. 初始化SimpleRetriever
  const retriever = new SimpleRetriever();
  
  // 2. 拆分所有内容为chunks
  const allChunks: Chunk[] = [];
  
  // 主页面
  const mainChunks = retriever.chunkContent(
    this.extractTextContent(mainPageContent),
    websiteUrl,
    'main'
  );
  allChunks.push(...mainChunks);
  
  // 子页面
  subPageContents.forEach(page => {
    const chunks = retriever.chunkContent(
      this.extractTextContent(page.content),
      page.url,
      'subpage'
    );
    allChunks.push(...chunks);
  });
  
  // 文档
  documentContents.forEach(doc => {
    const chunks = retriever.chunkContent(
      this.extractTextContent(doc.content),
      doc.url,
      'document'
    );
    allChunks.push(...chunks);
  });
  
  console.log(`[ContentRetriever] Total chunks created: ${allChunks.length}`);
  
  // 3. 建立BM25索引
  retriever.buildIndex(allChunks);
  
  // 4. 为每个关键词检索和调用LLM
  const keywordMatches: KeywordMatch[] = [];
  
  for (const keyword of keywords) {
    const debugLogger = new DebugLogger(keyword, websiteUrl);
    
    try {
      // 记录内容获取信息
      debugLogger.logContentFetch(
        mainPageContent.length,
        subPageContents.length,
        documentContents.length,
        mainPageContent.length + 
          subPageContents.reduce((sum, p) => sum + p.content.length, 0) +
          documentContents.reduce((sum, d) => sum + d.content.length, 0)
      );
      
      // 记录chunk信息
      debugLogger.logChunking(allChunks);
      
      // 检索相关chunks（最多30个）
      const retrievedChunks = retriever.retrieve(keyword, 30);
      debugLogger.logRetrieval(retrievedChunks);
      
      if (retrievedChunks.length === 0) {
        console.warn(`[ContentRetriever] No chunks retrieved for keyword: ${keyword}`);
        keywordMatches.push({
          keyword,
          found: false,
          occurrences: 0,
          contexts: []
        });
        continue;
      }
      
      // 构建优化后的prompt
      const prompt = this.buildOptimizedPrompt(keyword, retrievedChunks);
      
      // 调用LLM
      const llmStartTime = Date.now();
      const llmResponse = await this.callLLM(prompt);
      const llmDuration = Date.now() - llmStartTime;
      
      // 估算token数量（简单估算：中文1字符≈1.5tokens）
      const promptTokens = Math.ceil(prompt.length * 1.5);
      const completionTokens = Math.ceil(llmResponse.length * 1.5);
      debugLogger.logLLMCall(promptTokens, completionTokens, llmDuration);
      
      // 解析LLM响应
      const answer = this.parseLLMResponse(llmResponse);
      debugLogger.logLLMAnswer(answer);
      
      // 验证响应
      debugLogger.validate(retrievedChunks);
      
      // 转换为KeywordMatch格式
      keywordMatches.push({
        keyword: answer.keyword,
        found: answer.found,
        occurrences: answer.found ? 1 : 0,
        contexts: answer.found ? [answer.quotedSentence] : [],
        sourceUrl: answer.sourceUrl,
        confidence: answer.confidence
      });
      
      // 保存debug信息
      debugInfoList.push(debugLogger.getDebugInfo());
      
      // 输出debug信息到控制台
      if (process.env.DEBUG_MODE === 'true') {
        debugLogger.print();
      }
      
    } catch (error) {
      console.error(`[ContentRetriever] Error processing keyword ${keyword}:`, error);
      keywordMatches.push({
        keyword,
        found: false,
        occurrences: 0,
        contexts: [],
        error: this.getErrorMessage(error)
      });
    }
  }
  
  const totalDuration = Date.now() - startTime;
  console.log(`[ContentRetriever] Completed all keywords in ${totalDuration}ms`);
  
  // 构建文档结果（基于sourceUrl分组）
  const documentResults = this.buildDocumentResults(keywordMatches, documentContents);
  
  return {
    keywordMatches,
    documentResults,
    debugInfo: process.env.DEBUG_MODE === 'true' ? debugInfoList : undefined
  };
}

/**
 * 解析LLM响应
 */
private parseLLMResponse(llmResponse: string): any {
  try {
    // 提取JSON
    let content = llmResponse.trim();
    
    // 移除markdown代码块
    const jsonMatch = content.match(/```(?:json)?\s*(\{[\s\S]*?\})\s*```/);
    if (jsonMatch) {
      content = jsonMatch[1];
    }
    
    // 提取第一个{到最后一个}
    const jsonStart = content.indexOf('{');
    const jsonEnd = content.lastIndexOf('}');
    if (jsonStart !== -1 && jsonEnd !== -1 && jsonEnd > jsonStart) {
      content = content.substring(jsonStart, jsonEnd + 1);
    }
    
    return JSON.parse(content);
  } catch (error) {
    console.error('[ContentRetriever] Failed to parse LLM response:', error);
    throw new Error('Failed to parse LLM response as JSON');
  }
}

/**
 * 构建文档结果
 */
private buildDocumentResults(
  keywordMatches: KeywordMatch[],
  documentContents: Array<{ url: string; content: string }>
): DocumentRetrievalResult[] {
  const documentMap = new Map<string, KeywordMatch[]>();
  
  // 按sourceUrl分组
  keywordMatches.forEach(match => {
    if (match.found && match.sourceUrl) {
      const isDocument = documentContents.some(doc => doc.url === match.sourceUrl);
      if (isDocument) {
        if (!documentMap.has(match.sourceUrl)) {
          documentMap.set(match.sourceUrl, []);
        }
        documentMap.get(match.sourceUrl)!.push(match);
      }
    }
  });
  
  // 构建结果
  const results: DocumentRetrievalResult[] = [];
  documentMap.forEach((matches, url) => {
    results.push({
      documentUrl: url,
      status: 'success',
      keywordMatches: matches
    });
  });
  
  return results;
}
```


---

## 依赖安装

### 1. 安装natural库（BM25实现）

```bash
cd backend
npm install natural
npm install --save-dev @types/natural
```

### 2. package.json更新

```json
{
  "dependencies": {
    "natural": "^6.10.0"
  },
  "devDependencies": {
    "@types/natural": "^5.1.5"
  }
}
```

---

## 环境变量配置

### 新增环境变量

```bash
# .env
# Debug模式（开启后会输出详细的debug信息）
DEBUG_MODE=true

# 最大chunk数量限制
MAX_CHUNKS_PER_KEYWORD=30

# Chunk拆分配置
CHUNK_MAX_SIZE=500
CHUNK_MIN_SIZE=100
CHUNK_OVERLAP=50
```

---

## 测试方案

### 1. 单元测试

```typescript
// backend/src/services/__tests__/SimpleRetriever.test.ts

import { SimpleRetriever } from '../SimpleRetriever';

describe('SimpleRetriever', () => {
  let retriever: SimpleRetriever;
  
  beforeEach(() => {
    retriever = new SimpleRetriever();
  });
  
  describe('chunkContent', () => {
    it('should split content by paragraphs', () => {
      const content = '段落1内容。\n\n段落2内容。\n\n段落3内容。';
      const chunks = retriever.chunkContent(content, 'https://example.com', 'main');
      
      expect(chunks.length).toBeGreaterThan(0);
      expect(chunks[0].content).toContain('段落1');
    });
    
    it('should handle long paragraphs by splitting into sentences', () => {
      const longParagraph = '这是一个很长的段落。'.repeat(50);
      const chunks = retriever.chunkContent(longParagraph, 'https://example.com', 'main');
      
      expect(chunks.length).toBeGreaterThan(1);
      chunks.forEach(chunk => {
        expect(chunk.content.length).toBeLessThanOrEqual(500);
      });
    });
    
    it('should preserve metadata', () => {
      const content = '测试内容。';
      const chunks = retriever.chunkContent(content, 'https://example.com', 'main');
      
      expect(chunks[0].sourceUrl).toBe('https://example.com');
      expect(chunks[0].pageType).toBe('main');
      expect(chunks[0].chunkIndex).toBe(0);
    });
  });
  
  describe('retrieve', () => {
    beforeEach(() => {
      const chunks = [
        {
          id: '1',
          content: '金融机构应当建立反洗钱制度。',
          sourceUrl: 'https://example.com/page1',
          chunkIndex: 0,
          pageType: 'main'
        },
        {
          id: '2',
          content: '客户身份识别是反洗钱的重要环节。',
          sourceUrl: 'https://example.com/page2',
          chunkIndex: 0,
          pageType: 'subpage'
        },
        {
          id: '3',
          content: '数据保留期限应符合监管要求。',
          sourceUrl: 'https://example.com/page3',
          chunkIndex: 0,
          pageType: 'document'
        }
      ];
      
      retriever.buildIndex(chunks);
    });
    
    it('should retrieve relevant chunks', () => {
      const results = retriever.retrieve('反洗钱', 10);
      
      expect(results.length).toBeGreaterThan(0);
      expect(results[0].content).toContain('反洗钱');
    });
    
    it('should limit results to topK', () => {
      const results = retriever.retrieve('金融', 1);
      
      expect(results.length).toBe(1);
    });
    
    it('should return chunks with scores', () => {
      const results = retriever.retrieve('反洗钱', 10);
      
      results.forEach(chunk => {
        expect(chunk.score).toBeDefined();
        expect(typeof chunk.score).toBe('number');
      });
    });
  });
});
```

### 2. 集成测试

```typescript
// backend/src/services/__tests__/ContentRetriever.optimized.test.ts

import { ContentRetriever } from '../ContentRetriever';

describe('ContentRetriever - Optimized', () => {
  let retriever: ContentRetriever;
  
  beforeEach(() => {
    retriever = new ContentRetriever();
  });
  
  it('should use SimpleRetriever for keyword search', async () => {
    const result = await retriever.retrieveFromWebsite(
      'https://example.com',
      ['反洗钱', '客户身份识别']
    );
    
    expect(result.status).toBe('success');
    expect(result.keywordMatches.length).toBe(2);
    
    // 验证返回了引用句子和sourceUrl
    result.keywordMatches.forEach(match => {
      if (match.found) {
        expect(match.contexts.length).toBeGreaterThan(0);
        expect(match.sourceUrl).toBeDefined();
      }
    });
  });
  
  it('should include debug info when DEBUG_MODE is enabled', async () => {
    process.env.DEBUG_MODE = 'true';
    
    const result = await retriever.retrieveFromWebsite(
      'https://example.com',
      ['反洗钱']
    );
    
    expect(result.debugInfo).toBeDefined();
    expect(result.debugInfo![0].keyword).toBe('反洗钱');
    expect(result.debugInfo![0].retrieval).toBeDefined();
    expect(result.debugInfo![0].llmCall).toBeDefined();
  });
  
  it('should limit chunks to MAX_CHUNKS_PER_KEYWORD', async () => {
    process.env.MAX_CHUNKS_PER_KEYWORD = '30';
    process.env.DEBUG_MODE = 'true';
    
    const result = await retriever.retrieveFromWebsite(
      'https://example.com',
      ['金融']
    );
    
    const debugInfo = result.debugInfo![0];
    expect(debugInfo.retrieval.retrievedChunks.length).toBeLessThanOrEqual(30);
  });
});
```


---

## 实施步骤

### 第1天：基础设施

**任务**:
1. ✅ 安装natural库
2. ✅ 创建SimpleRetriever类文件
3. ✅ 创建DebugLogger类文件
4. ✅ 添加环境变量配置

**验收标准**:
- 依赖安装成功
- 文件结构正确
- 环境变量可读取

---

### 第2-3天：SimpleRetriever实现

**任务**:
1. ✅ 实现chunk拆分逻辑
2. ✅ 实现BM25索引构建
3. ✅ 实现检索功能
4. ✅ 编写单元测试

**验收标准**:
- 能正确拆分内容为chunks
- BM25检索返回相关结果
- 单元测试通过率100%

---

### 第4-5天：DebugLogger实现

**任务**:
1. ✅ 实现debug信息收集
2. ✅ 实现验证逻辑
3. ✅ 实现输出和保存功能
4. ✅ 编写单元测试

**验收标准**:
- 能收集完整的debug信息
- 验证逻辑正确
- 可输出JSON格式的debug信息

---

### 第6-7天：集成到ContentRetriever

**任务**:
1. ✅ 修改llmWebSearch方法
2. ✅ 集成SimpleRetriever
3. ✅ 集成DebugLogger
4. ✅ 优化LLM prompt
5. ✅ 编写集成测试

**验收标准**:
- 完整流程可运行
- LLM返回包含引用句子和URL
- 集成测试通过

---

### 第8-9天：测试和优化

**任务**:
1. ✅ 端到端测试
2. ✅ 性能测试
3. ✅ 成本分析
4. ✅ Bug修复

**验收标准**:
- 所有测试通过
- 性能达标（响应时间<30秒）
- 成本降低50%以上

---

### 第10天：文档和部署

**任务**:
1. ✅ 更新API文档
2. ✅ 更新README
3. ✅ 部署到测试环境
4. ✅ 验证部署结果

**验收标准**:
- 文档完整
- 测试环境运行正常
- 可以通过API测试

---

## 验收标准

### 功能验收

**1. Chunk拆分**
- ✅ 能按段落和句子正确拆分
- ✅ Chunk大小在100-500字符之间
- ✅ 保留完整的元数据

**2. BM25检索**
- ✅ 能检索到相关chunks
- ✅ 结果按相关性排序
- ✅ 限制返回数量不超过30

**3. LLM调用**
- ✅ 使用优化后的prompt
- ✅ 返回包含引用句子
- ✅ 返回包含sourceUrl
- ✅ 返回JSON格式正确

**4. Debug工具**
- ✅ 收集完整的debug信息
- ✅ 验证LLM响应的准确性
- ✅ 可输出和保存debug信息

### 性能验收

**1. 响应时间**
- ✅ 单个关键词<10秒
- ✅ 5个关键词<30秒
- ✅ 10个关键词<60秒

**2. 成本**
- ✅ 单次查询Token数<8000
- ✅ 单次查询成本<$0.08
- ✅ 相比当前降低50%以上

**3. 准确率**
- ✅ 关键词识别准确率>80%
- ✅ 引用句子准确率>90%
- ✅ sourceUrl准确率100%

---

## 风险和应对

### 风险1：BM25检索效果不佳

**表现**:
- 检索到的chunks不相关
- 关键词匹配率低

**应对**:
- 调整chunk大小
- 增加中文分词
- 考虑使用jieba分词

### 风险2：LLM不遵守prompt规则

**表现**:
- 不返回引用句子
- 返回格式不正确
- 编造不存在的内容

**应对**:
- 强化prompt约束
- 添加few-shot示例
- 增加验证和重试机制

### 风险3：性能未达预期

**表现**:
- 响应时间仍然较长
- 成本降低不明显

**应对**:
- 进一步减少chunk数量
- 优化chunk拆分策略
- 考虑缓存机制

---

## 监控指标

### 关键指标

**1. 检索质量**
- 平均检索chunks数量
- 平均BM25分数
- Top chunk相关性

**2. LLM性能**
- 平均Token数量
- 平均响应时间
- 平均成本

**3. 结果质量**
- 引用句子验证通过率
- sourceUrl验证通过率
- LLM置信度分布

### 告警规则

**P1告警**:
- 引用句子验证失败率>10%
- 平均响应时间>30秒
- 单次查询成本>$0.10

**P2告警**:
- LLM置信度<0.5的比例>20%
- 检索到的chunks数量<5
- BM25平均分数<0.1

---

## 回滚方案

### 触发条件
- 准确率下降>20%
- 响应时间增加>50%
- 出现严重bug影响服务

### 回滚步骤
1. 切换到旧版本代码
2. 重启服务
3. 验证功能正常
4. 通知相关人员

### 回滚后处理
1. 分析失败原因
2. 修复问题
3. 在测试环境重新验证
4. 制定新的上线计划

---

## 总结

### 核心改进
1. **简单检索层**: 使用BM25减少LLM输入量
2. **优化Prompt**: 强制引用原文和URL
3. **Debug工具**: 提供完整的可调试性
4. **成本优化**: 预计降低50-60%

### 预期效果
- Token数量: 15000 → 6000 (60%↓)
- 响应时间: 30-60秒 → 20-40秒 (30%↓)
- 准确率: 70% → 80% (10%↑)
- 可调试性: 大幅提升

### 下一步
完成阶段一后，可以继续实施：
- 阶段二: 向量检索和Reranker
- 阶段三: 智能爬取和页面评分
- 阶段四: 验证层和质量保证

---

**文档版本**: 1.0  
**最后更新**: 2026-04-30  
**负责人**: 开发团队
